import { createSlice, ReducerType } from "@reduxjs/toolkit";

export const FlightsearchSlice = createSlice({
    name: "FlightSearchdata",
    initialState: {
        flightstatedata=[],
        reducers: (state, action) => {
            state.FlightsearchSlice
        }
    }
    
})